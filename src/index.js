import './styles/style.scss'
import {cardGame} from  "./cards";
import {angular,docker,firebase, js,back,materialize,mongodb,nest,node, php, py, react, rx, svelte, tailwind, typescript, vscode,vue} from "./images"
cardGame('#game')

const img = document.createElement('img')
